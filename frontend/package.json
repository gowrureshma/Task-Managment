{
  "name": "team-task-manager-frontend",
  "version": "1.0.0",
  "description": "React frontend for Team Task Manager application",
  "private": true,

  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.11.0",
    "axios": "^1.4.0",
    "react-scripts": "5.0.1",
    "serve": "^14.2.1"
  },

  "devDependencies": {
    "tailwindcss": "^3.3.1",
    "postcss": "^8.4.24",
    "autoprefixer": "^10.4.14"
  },

  "scripts": {
    "start": "serve -s build -l 3000",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },

  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },

  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],

    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
